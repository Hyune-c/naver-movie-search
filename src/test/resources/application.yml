spring:
  h2:
    console:
      enabled: true
  config:
    activate:
      on-profile: test
logging:
  level:
    root: debug
    org:
      hibernate:
        type:
          descriptor:
            sql: trace
server:
  servlet:
    encoding:
      charset: utf-8
      force: true
